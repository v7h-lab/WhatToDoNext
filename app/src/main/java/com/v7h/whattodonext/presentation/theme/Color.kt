package com.v7h.whattodonext.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary colors - bright green accent
val Primary = Color(0xFF38E07B)

// Background colors
val BackgroundLight = Color(0xFFF6F8F7)
val BackgroundDark = Color(0xFF122017)

// Content colors (text)
val ContentLight = Color(0xFF111714)
val ContentDark = Color(0xFFF6F8F7)

// Subtle colors (secondary text)
val SubtleLight = Color(0xFF648772)
val SubtleDark = Color(0xFFA0B5A9)

// Surface colors (cards, containers)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1A2C20)

// Border colors
val BorderLight = Color(0xFFE3E8E5)
val BorderDark = Color(0xFF2A4234)

// Error colors
val ErrorLight = Color(0xFFFF5252)
val ErrorDark = Color(0xFFFF8A80)

// Additional colors for UI components
val DeclineButton = Color(0xFFFF5252) // Red for decline
val AcceptButton = Primary // Green for accept
val SaveButton = Color(0xFF9E9E9E) // Gray for save

// Dark theme colors
val DarkOnBackground = ContentDark
val DarkOnSurface = ContentDark
val DarkOnPrimary = Color.Black

// Light theme colors  
val LightOnBackground = ContentLight
val LightOnSurface = ContentLight
val LightOnPrimary = Color.White
